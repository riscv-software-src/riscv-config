### 
# **Description**: Vendor name.
#
# **Examples**:
#
# .. code-block:: none
#
#   Vendor: Shakti
#   Vendor: Incoresemi
#
# **Constraints**:
#   - None
#
# `------------------------------------------------------------------------------------------------------------------------------`
#
Vendor: 
    type: string

### 
# **Description**: Device Name.
#
# **Examples**:
#
# .. code-block:: none
#
#   Device: E-Class
#   Device: C-Class
#
# **Constraints**:
#   - None
#
# `------------------------------------------------------------------------------------------------------------------------------`
#
Device:
    type: string

###
# **Description**: Takes input a string representing the ISA supported by the implementation. All extension names
# (other than Zext) should be mentioned in upper-case. Z extensions should begin with an upper-case
# 'Z' followed by lower-case extension name (without Camel casing)
#
# **Examples**: 
#
# .. code-block:: none
#
#   ISA: RV32IMA 
#   ISA: RV64IMAFDCZifencei
#
# **Constraints**: 
#   - Certain extensions are only valid in certain user-spec version. For, eg. Zifencei is available only in user-spec 2.3 and above.
#   - The ISA string must be specified in the manner as specified in the specifications(like subsequent Z extensions must be separated with an '_')
# 
# `------------------------------------------------------------------------------------------------------------------------------`
#
ISA: 
    type: string
    regex: '^RV(32|64|128)[IE]+[ACDEFGIJLMNPQSTUVX]*(Zicsr|Zifencei|Zam|Ztso|){,1}(_Zifencei){,1}(_Zam){,1}(_Ztso){,1}$'
    required: True
    check_with: capture_isa_specifics

### 
# **Description**: Version number of User/Non-priveleged ISA specification as string. Please enclose the version in "" to avoid type mismatches.
#
# **Examples**:
#
# .. code-block:: none
#
#   User_Spec_Version: "2.2"
#   User_Spec_Version: "2.3"
#
# **Constraints**:
#   - should be a valid version later than 2.2
#
# `------------------------------------------------------------------------------------------------------------------------------`
#
User_Spec_Version:
    type: string
    default: "2.2"
    required: True
    check_with: user_version_check

### 
# **Description**: Version number of Priveleged ISA specification as string. Please enclose the version in "" to avoid type mismatches.
#
# **Examples**:
#
# .. code-block:: none
#
#   Privilege_Spec_Version: "1.10"
#   Privilege_Spec_Version: "1.11"
#
# **Constraints**:
#   - should be a valid version later than 1.10 
#
# `------------------------------------------------------------------------------------------------------------------------------`
#
Privilege_Spec_Version:
    type: string
    default: "1.10"
    required: True
    check_with: priv_version_check

###
# **Description**: A boolean value indicating whether hardware support for misaligned load/store requests exists.
# 
# **Examples**:
# 
# .. code-block:: none
# 
#    hw_data_misaligned_support: True
#    hw_data_misaligned_support: False
# 
# **Constraints**:
# - None
# 
# `------------------------------------------------------------------------------------------------------------------------------`
# 
hw_data_misaligned_support:
    type: boolean
    default: False

xlen:
    type: integer
    allowed: [32,64,128]
    readonly: True

misa:
  type: dict
  schema:
    description:
      type: string
      default: This is a read-write register reporting the ISA supported by the hart.
    address:
      type: integer
      default: 0x301
      allowed: [0x301]
    priv_mode:
      type: string
      allowed: ['M']
      default: 'M'
    reset-val:
      type: integer
    rv32:
      type: dict
      schema:
        exists:
          type: boolean
          default: False
        fields:
          type: list
        mxl:
          type: dict
          schema:
            description:
              type: string
              default: This field encodes the native base integer ISA width.
            msb:
              type: integer
              default: 31
              allowed: [31]
            lsb:
              type: integer
              default: 30
              allowed: [30]
            reset-val:
              type: integer
            implemented:
              type: boolean
              default: True
        extensions:
          type: dict
          schema:
            description:
              type: string
              default: The Extensions field encodes the presence of the standard extensions, with a single bit per letter of the alphabet.
            msb:
              type: integer
              default: 25
              allowed: [25]
            lsb:
              type: integer
              default: 0
              allowed: [0]
            reset-val:
              type: integer
            implemented:
              type: boolean
              default: True
      default:
        exists: False
    rv64:
      type: dict
      schema:
        exists:
          type: boolean
          default: False
        fields:
          type: list
        mxl:
          type: dict
          schema:
            description:
              type: string
              default: This field encodes the native base integer ISA width.
            msb:
              type: integer
              default: 63
              allowed: [63]
            lsb:
              type: integer
              default: 62
              allowed: [62]
            reset-val:
              type: integer
            implemented:
              type: boolean
              default: True
        extensions:
          type: dict
          schema:
            description:
              type: string
              default: The Extensions field encodes the presence of the standard extensions, with a single bit per letter of the alphabet.
            msb:
              type: integer
              default: 25
              allowed: [25]
            lsb:
              type: integer
              default: 0
              allowed: [0]
            reset-val:
              type: integer
            implemented:
              type: boolean
              default: True
      default:
        exists: False
    rv128:
      type: dict
      schema:
        exists:
          type: boolean
          default: False
        fields:
          type: list
        mxl:
          type: dict
          schema:
            description:
              type: string
              default: This field encodes the native base integer ISA width.
            msb:
              type: integer
              default: 127
              allowed: [127]
            lsb:
              type: integer
              default: 126
              allowed: [126]
            reset-val:
              type: integer
            implemented:
              type: boolean
              default: True
        extensions:
          type: dict
          schema:
            description:
              type: string
              default: The Extensions field encodes the presence of the standard extensions, with a single bit per letter of the alphabet.
            msb:
              type: integer
              default: 25
              allowed: [25]
            lsb:
              type: integer
              default: 0
              allowed: [0]
            reset-val:
              type: integer
            implemented:
              type: boolean
              default: True
      default:
        exists: False
  default:
    address: 0x301